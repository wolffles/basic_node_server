#Notes on the basic node server

## what it is. 
    a node application that uses express to create a server. entry is index.js.

## START ( local host only works on chrome... for somereason)
    `node index.js`